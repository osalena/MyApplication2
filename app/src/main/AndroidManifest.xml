<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogIn.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".LogIn.RegisterActivity" />
        <activity
            android:name=".LogIn.activity_facebook_login"
            android:label="@string/title_activity_facebook_login" />
        <activity android:name=".activity_main_cookpad" />
        <activity android:name=".LogIn.activity_signIn" />
        <activity android:name=".Category.Category_list_details" />
        <activity android:name=".MyReceipts.CreateReceiptActivity" />
        <activity android:name=".MyReceipts.MyReceiptsActivity" />
        <activity android:name=".MyNotificationsActivity" />
        <activity android:name=".MyConversationsActivity" />
    </application>

</manifest>